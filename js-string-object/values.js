const computer = {
    brand: 'Hp',
    price: 30000,
    processor: 'intel',
    ssd : '512gb',
    monitor: '21 ins',
}

const values = Object.values  (computer)
const keys = Object.keys(computer)
console.log(values,keys);